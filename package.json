{
  "name": "fun-walk",
  "version": "1.0.0",
  "private": true,
  "description": "Fun-Walk — планировщик маршрутов прогулок",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --kill-others -n backend,frontend -c blue,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start:prod": "npm run start:prod --prefix backend",
    "preview": "npm run preview --prefix frontend",
    "coursework": "powershell -ExecutionPolicy Bypass -File coursework/build.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
